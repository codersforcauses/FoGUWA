{
  "version": 2,
  "builds": [
    {
      "src": "nuxt.config.js",
      "use": "@nuxtjs/now-builder",
      "config": {
        "serverFiles": ["middleware/**", "server/**"]
      },
      "env": {
        "GMAPS_KEY": "@gmap-api"
      },
      "build": {
        "env": {
          "GMAPS_KEY": "@gmap-api"
        }
      }
    }
  ]
}
